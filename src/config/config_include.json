{
  "include_files": ["config/config_automode.json"],

  "execution": {
    "seed_window_size": 10,
    "seed_window_movement": 2,
    "budget": 500
  },

  "logging": {
    "snapshot_frequency": 100,
    "log_level": "DEBUG"
  },

  "parallelization": {
    "parallel": 5,
    "mode": "MPI"
  }
}
